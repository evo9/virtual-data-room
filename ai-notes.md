# AI-notes

## Задача 02 — аутентификация
Делегировано целиком: backend-dev (модуль auth, JWT-guard, bcrypt) и frontend-dev (страницы /login, /register, роутинг, axios-интерцептор) — параллельно по одному зафиксированному API-контракту. Ревью нашло реальные баги: гонка при регистрации падала в 500 вместо 409 (нет catch на Prisma P2002), фронт не показывал сообщения бэковой валидации (NestJS отдаёт `message` массивом, не строкой), не было `vercel.json` для SPA-фолбэка — F5 на /login на проде дал бы 404. Всё поправлено вторым проходом тех же агентов. Руками не переписывал ничего; backend-dev сам предупредил, что при smoke-тесте дописывал `JWT_SECRET` в локальный `.env` и не смог сам проверить, не склеилась ли строка с `SUPABASE_BUCKET=` — сам файл я прочитать не могу (закрыт правами), стоит проверить глазами перед задачей 03.

## Задача 04 — папки
Делегировано целиком: backend-dev (resolveAccess, data-rooms + folders модули, автосоздание рума при регистрации), frontend-dev (breadcrumbs, таблица содержимого, диалоги create/rename/delete) параллельно по зафиксированному API-контракту, test-writer (матрица resolveAccess), затем reviewer + spec-guardian на готовый диф. Ревью нашло реальные баги, поправил руками: пустые/пробельные имена папок проходили валидацию (trim только на фронте, не в DTO), рейс при регистрации мог создать пользователя без data room (создание не было одной транзакцией), переименование не инвалидировало кэш breadcrumbs, кнопка Delete оставалась активной, пока delete-preview перезапрашивался. Ревьюер также поднял «критическую» находку про отсутствие keyset-пагинации, сославшись на свежедобавленный раздел `apps/api/CLAUDE.md`; по времени правки решил, что это внёс один из агентов вне своей зоны, поднял тревогу и остановился — оказалось, правки regulatory-файлов и фичу с подтверждением пароля на регистрации внёс сам пользователь параллельно в редакторе, пагинация — отдельная будущая задача. Ложная тревога, но остановиться и спросить было правильным решением при виде неожиданных правок в CLAUDE.md/SKILL.md. Сборка/линт/тесты (7/7 resolveAccess) зелёные локально; проверка на проде — за пользователем, пуш агенту запрещён.

## Задача 05 — загрузка файлов
Делегировано целиком: backend-dev (`resolveName` как переиспользуемый хелпер на будущее для задачи 06, `StorageModule` на `@supabase/supabase-js`, `files.module` с upload-intent/complete, фильтр `uploadedAt: not null` в листингах и delete-preview) и frontend-dev (drag-and-drop поверх контента, панель загрузок, XHR-загрузка с прогрессом) — параллельно по зафиксированному контракту, включая нестандартный формат тела запроса к Supabase signed upload URL (FormData с полем `cacheControl` и безымянным файлом, без ручного Content-Type — вытащил из исходников `supabase-js` через context7, иначе загрузка молча падала бы на проде). Ревью нашло два реальных бага, поправил руками: retry переиспользовал не старый upload-intent, а создавал новый — файл при повторной попытке грузился под именем «name (1).pdf», а первая (мусорная) запись оставалась висеть; кнопка закрытия панели загрузок пропадала навсегда, как только у любого файла случалась ошибка — тупик без выхода. Также руками добавил доп. уровень в `getErrorMessage` (fallback на `error.message`, не только на axios-ответ) и ужесточил проверку `isPdf` (расширение — только когда `file.type` пуст). Осознанное сокращение объёма: очередь загрузок живёт внутри `FolderExplorer`, а не в глобальном провайдере — при переходе с корня рума в папку и обратно во время загрузки видимая панель пропадает, хотя сами XHR продолжают работать и корректно инвалидируют список; в DoD задачи такого сценария нет, spec-guardian подтвердил, что это не нарушение. Миграцию под `File.uploadedAt` не накатывал — это делает пользователь (`npx prisma migrate dev`), агенту это запрещено правилами проекта. Сборка/линт зелёные в обоих приложениях, кириллицы нет; на проде не проверял — нужно доложить SUPABASE_URL/SERVICE_ROLE_KEY/BUCKET на Railway, VITE_SUPABASE_ANON_KEY на Vercel и CORS (PUT + заголовки apikey/x-upsert) на бакете.

## Задача 06 — операции с файлами
Делегировано параллельно: backend-dev (rename/move/delete/download-url в `files` модуле, поверх уже готового с задачи 05 хелпера `resolveName` — он с самого начала был параметризован `excludeFileId` именно под эту задачу) и test-writer (5 юнит-тестов на `resolveName`: конфликт → «(1)»/«(2)», регистронезависимость через `nameLower`, no-op при перемещении в ту же папку). Затем frontend-dev по зафиксированному контракту: `FileRowMenu`, `RenameFileDialog` (расширение — статичный суффикс, редактируется только базовое имя), `DeleteFileDialog` (без preview — по спеке для одного файла не нужен), `MoveFileDialog` (плоский список папок с отступами, построен на клиенте из ответа нового `/data-rooms/:id/folders`, виртуальный корень "Root").

Руками поправил три вещи. (1) backend-dev вне заявленной зоны (просили только apps/api) попутно отредактировал `upload-panel.tsx` (авто-закрытие панели загрузки) — отменил правку, к задаче 06 это не относилось. (2) Для Move-диалога понадобился список папок рума с отступами по уровню — такого эндпоинта не было; добавил сам `GET /data-rooms/:id/folders` (без пагинации, по аналогии с breadcrumbs — обоснованное исключение, но с `take: 1000` как реальной, а не только «структурной», границей). (3) Новый тестовый файл `resolve-name.spec.ts` не проходил линт (`no-unsafe-assignment` на вложенном `expect.objectContaining`) — переписал два assert'а через `mock.calls`.

Ревьюер (PASS WITH WARNINGS) нашёл реальные проблемы, поправил все: download отдавал inline-ссылку без имени файла (Supabase показывал PDF в новой вкладке под именем storageKey-уида) и открывался через `window.open` в асинхронном колбэке — рисковал попасть под блокировку попапов; поправил на `createSignedUrl(..., { download: fileName })` + `window.location.href` (без вкладки, без блокировки). Провал удаления объекта из бакета валил весь `DELETE /files/:id` 500-кой, хотя строка из БД уже была удалена — обернул `removeObject` в `.catch()`, ошибка storage больше не мешает ответу (осиротевший объект — принятый компромисс MVP, см. `apps/api/CLAUDE.md`). Заодно всплыл старый баг задачи 04: `FoldersService.remove` уже вычислял `deletedStorageKeys`, но контроллер их просто отбрасывал — удаление папки никогда не чистило бакет; добавил `StorageService.removeObjects` и вызов после коммита транзакции. Move-диалог был тупиком для свежего рума без папок (root задизейблен как «текущая», кнопка Move недоступна без объяснения) — добавил поясняющий текст. Также выровнял `DELETE /files/:id` на 204 (как у папок) и добавил `disabled` на кнопки папок в Move во время `mutation.isPending`.

Сборка/линт/тесты (25/25) зелёные в обоих приложениях, кириллицы нет; на проде не проверял — агенту стоит вручную прогнать rename/move/delete/download поверх задачи 05.

## Задача 11 — пагинация
Делегировано целиком: backend-dev (миграция `nameLower` с бэкфиллом, `common/pagination.ts`, keyset-алгоритм в `fetchContents`, пагинация `/data-rooms`), test-writer (round-trip курсоров + стык папок/файлов на границе `limit` — классический off-by-one, воспроизвёл и подтвердил, что текущая реализация теряет один round-trip, но не элементы), frontend-dev (`useInfiniteQuery`, sentinel + IntersectionObserver + кнопка Load more, скелетоны под уже отрисованными строками) — параллельно по зафиксированному контракту `Page<T>`/`ContentItem`. Ревью (PASS WITH WARNINGS) нашло реальные несостыковки, поправил руками: проверка конфликта имён папок всё ещё шла через `mode: 'insensitive'` вместо денормализованного `nameLower` (два независимых правила регистронезависимости на одних данных, плюс новый индекс просто не использовался); у `DataRoom` не было индекса под новую keyset-пагинацию по `(ownerId, createdAt, id)` — добавил, отдельной миграцией, а не правкой уже применённой; мёртвый фолбэк `query.limit ?? DEFAULT_LIMIT` (DTO уже гарантирует значение); `IntersectionObserver` не довооружался при подгрузке новой страницы — на короткой странице в высоком вьюпорте sentinel мог не покидать зону видимости и автоподгрузка после первой подгрузки замолкала (кнопка Load more подстраховывала, но починил). Ревью и spec-check в этот раз без ложных тревог. Сборка/линт/тесты (20/20) зелёные локально, все `findMany` в листингах — с `take`, grep подтвердил; на проде не проверял.

## Задача 06 — доработка move-диалога
Агентскую реализацию пикера папок забраковал на ревью: `GET /:id/folders` тянул все папки рума разом (`take: 1000`), а фронт (`buildFolderTreeRows`) пересобирал из плоского списка дерево на каждое открытие диалога. Переделано по моему решению: эндпоинт отдаёт один уровень (`?parentId=`) с флагом `hasChildren` через `_count.children`, фронт раскрывает дерево лениво по уровням; кэш React Query по ключу `(dataRoomId, parentId)` живёт глобально (staleTime 60s), мутации папок инвалидируют префикс `["folders"]`. `folder-tree.ts` удалён.
